spring.application.name=Stock market simulator

# JWT
jwt.public.key=classpath:test_jwt_public.key
jwt.private.key=classpath:test_jwt_private.key
jwt.key.id=RANDOM_KEY_ID

server.servlet.context-path=/api

#proxy
app.proxy-config.enabled=false